# {{ project_slug }}

A new MLOps project created from the template.

## Getting Started

### Prerequisites

- Python {{ python_version }}+
- `uv` (Python package manager)
{% if include_feature_engineering -%}
- `graphviz` (for visualizing Hamilton pipelines if you enabled feature engineering)

To install `graphviz` on Debian/Ubuntu:
```bash
sudo apt update && sudo apt install -y graphviz
```
On macOS (using Homebrew):
```bash
brew install graphviz
```
{% endif -%}

### Installation

1) **Clone the repository**
```bash
git clone <your-repo-url>
cd <your-repo-name>
```

2) **Install dependencies**
```bash
make install
```

3) **Verify the setup**
```bash
make verify
```

## Configuration

Before running the pipelines, you may configure the following environment variables:

- `BASE_DATA_PATH`: Path to store input/output data (defaults to `data/`).
- `ARTIFACT_PATH`: Path to store model artifacts (defaults to `artifacts/`).
- `WANDB_KEY`: Your Weights & Biases API key (optional).
- `USERNAME`: A username for Metaflow to namespace its runs (e.g., `export USERNAME=$(whoami)`).

## Usage

This project ships with several Metaflow pipelines{% if api_framework == 'fastapi' %} and an API{% endif %}:

- **Collect Data**: `make collect-data`
{% if include_feature_engineering -%}
- **Build Features**: `make build-features`
{% endif -%}
- **Train Model**: `make train-model`
{% if include_monitoring -%}
- **Monitor Model**: `make monitor-model`
{% endif -%}

{% if api_framework == "fastapi" -%}
To run the API locally:
```bash
uv run api
```
{% endif -%}

{% if cloud_provider == "gcp" -%}
## CI/CD Pipeline

This project uses a CI/CD pipeline with GitHub Actions and Google Cloud Build.
A single Docker image is built to run multiple services (API, training jobs, etc.), simplifying deployments.

### Pipeline Architecture

```mermaid
%%{init: {
  "theme": "base",
  "themeVariables": {
    "fontFamily": "Inter, ui-sans-serif, system-ui, Segoe UI, Roboto, Helvetica, Arial",
    "primaryColor": "#0f172a",
    "primaryTextColor": "#f8fafc",
    "primaryBorderColor": "#334155",
    "lineColor": "#94a3b8",
    "edgeLabelBackground": "#0f172a",
    "clusterBkg": "#111827",
    "clusterBorder": "#334155"
}
}%%

flowchart TB
    A[👨‍💻 Developer] -->|Push code| B[📁 GitHub Repository]
    B -->|Pull request| C[🔄 GitHub Actions CI]
    C -->|Tests pass| D[✅ Merge to Main]
    D -->|Trigger build| E[🏗️ Google Cloud Build]

    E --> F[🐳 Build Unified Docker Image]
    F --> G[📦 Push to Artifact Registry]
    G --> H

    subgraph H[☁️ Google Cloud]
      direction TB
      X[Single Image → Multiple Deployments] --> {% if api_framework == 'fastapi' %}I[🌐 API Service<br/>ENTRYPOINT_COMMAND=api]
      X --> {% endif %}J[🤖 Training Job<br/>ENTRYPOINT_COMMAND=train-model]
      X --> K[📊 Data Collection Job<br/>ENTRYPOINT_COMMAND=collect-data]
{% if include_feature_engineering -%}
      X --> L[⚙️ Feature Engineering Job<br/>ENTRYPOINT_COMMAND=build-features]
{% endif -%}
{% if include_monitoring -%}
      X --> M[📈 Model Monitoring Job<br/>ENTRYPOINT_COMMAND=monitor-model]
{% endif -%}
    end

    classDef dev fill:#1d4ed8,stroke:#93c5fd,color:#f8fafc,stroke-width:2.5px;
    classDef ci fill:#0ea5e9,stroke:#7dd3fc,color:#0b1220,stroke-width:2.5px;
    classDef merge fill:#10b981,stroke:#6ee7b7,color:#062418,stroke-width:2.5px;
    classDef build fill:#a855f7,stroke:#c084fc,color:#1f0a33,stroke-width:2.5px;
    classDef image fill:#f59e0b,stroke:#fcd34d,color:#1a1200,stroke-width:2.5px;
    classDef registry fill:#f97316,stroke:#fdba74,color:#1a0d00,stroke-width:2.5px;
    classDef group fill:#111827,stroke:#334155,color:#e5e7eb,stroke-width:2.5px;
    classDef svc fill:#0f766e,stroke:#34d399,color:#ecfeff,stroke-width:2.5px;
    classDef job fill:#be123c,stroke:#fda4af,color:#f8fafc,stroke-width:2.5px;

    class A,B dev;
    class C ci;
    class D merge;
    class E build;
    class F image;
    class G registry;
    class H group;
{% if api_framework == 'fastapi' -%}
    class I svc;
{% endif -%}
    class J,K{% if include_feature_engineering %},L{% endif %}{% if include_monitoring %},M{% endif %} job;
```
{% endif -%}