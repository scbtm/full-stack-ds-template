# TEMPLATE PLACEHOLDER:
# Logical steps that combine core functions for data collection.

from {{ package_name }}.data_collection.functions import (
    load_sample_data,
    validate_data_quality
)
from {{ package_name }}.config import config


def collect_and_validate_data():
    """
    Complete data collection step: load, validate, and save data.

    Returns:
        Path to saved data.
    """
    print("Starting data collection step...")

    # Load sample data
    data = load_sample_data()
    print(f"Loaded {len(data)} rows of data")

    # Validate data quality
    if not validate_data_quality(data):
        raise ValueError("Data failed quality validation")

    return data