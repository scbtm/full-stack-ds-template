{% if include_sample_code == "starter" -%}
# TEMPLATE PLACEHOLDER:
# Metaflow training pipeline.

from metaflow import FlowSpec, step


class TrainingFlow(FlowSpec):
    """A generic Metaflow pipeline for model training."""

    @step
    def start(self):
        print("Starting model training flow...")
        self.next(self.train_model)

    @step
    def train_model(self):
        print("Training model...")
        # TODO(template): Add your training logic here
        self.next(self.end)

    @step
    def end(self):
        print("Model training flow finished.")


def main():
    TrainingFlow()


if __name__ == "__main__":
    main()
{% else -%}
"""
Modeling Metaflow pipeline for {{ package_name }}.

This module contains the Metaflow pipeline for model training.
TODO(template): Implement your modeling pipeline here.
"""

from metaflow import FlowSpec, step


class TrainingFlow(FlowSpec):
    """Metaflow pipeline for model training."""

    @step
    def start(self):
        """Start the training flow."""
        print("Starting model training flow...")
        # TODO(template): Implement your flow logic here
        self.next(self.end)

    @step
    def end(self):
        """End the training flow."""
        print("Model training flow finished.")


def main():
    """Entry point for the training flow."""
    TrainingFlow()


if __name__ == "__main__":
    main()
{% endif -%}